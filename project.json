{
  "$schema": "../../schemas/project.schema.json",
  "name": "audiolab-farm",
  "type": "monorepo",
  "default_safety_tier": "dangerous",
  "paths": {
    "runtime_dir": "apps/playground/.agent"
  },
  "primary_repo": "/Volumes/c910ssd/dev/audiolab",
  "worktree_base": "/Volumes/c910ssd/dev/audiolab-wt",
  "apps": ["apps/playground", "apps/sherpa-voice", "apps/minimal"],
  "fixtures": {
    "templates": [
      {
        "dst": "CLAUDE.local.md",
        "compose": {
          "var": "FLOW_TYPE",
          "variants": {
            "": {
              "includes": [
                "CLAUDE.local.base.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "custom": {
              "includes": [
                "CLAUDE.local.custom.md",
                "CLAUDE.local.base.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "fix-bug": {
              "includes": [
                "CLAUDE.local.agent.fix-bug.md",
                "CLAUDE.local.base.md",
                "CLAUDE.local.agent.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "new-feature": {
              "includes": [
                "CLAUDE.local.agent.feature.md",
                "CLAUDE.local.base.md",
                "CLAUDE.local.agent.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "review-pr": {
              "includes": [
                "CLAUDE.local.agent.review-pr.md",
                "CLAUDE.local.base.md",
                "CLAUDE.local.agent.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "pr-complete": {
              "includes": [
                "CLAUDE.local.base.md",
                "CLAUDE.local.agent.md",
                ".agent/agentic-toolkit.md"
              ]
            },
            "update-branch": {
              "includes": [
                "CLAUDE.local.base.md",
                "CLAUDE.local.agent.md",
                ".agent/agentic-toolkit.md"
              ]
            }
          }
        }
      },
      {
        "src": ".agent/agentic-toolkit.md",
        "dst": ".agent/agentic-toolkit.md"
      }
    ]
  },
  "hooks": {
    "setup": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; yarn install --immutable && cd apps/playground && yarn build:deps",
    "preflight": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; PLATFORM={{platform}} IOS_SIMULATOR={{simulator}} ADB_SERIAL={{adb_serial}} bash {{repo}}/apps/playground/scripts/agentic/preflight.sh",
    "health_check": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; bash {{repo}}/apps/playground/scripts/agentic/app-state.sh --device {{simulator}} route",
    "dev_server_check": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; lsof -nP -iTCP:{{port}} -sTCP:LISTEN >/dev/null 2>&1",
    "recipe_action_manifest": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; cd {{repo}}/apps/playground && node scripts/agentic/recipe-v1/run-recipe-v1.mjs manifest",
    "recipe_doctor": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; cd {{repo}}/apps/playground && yarn recipe:doctor --json | node -e 'let s=\"\"; process.stdin.on(\"data\",d=>s+=d); process.stdin.on(\"end\",()=>{const d=JSON.parse(s); const pass=d.status===\"pass\"; console.log(JSON.stringify({runner_protocol_version:1,status:pass?\"pass\":\"fail\",checks:[{id:\"expo_recipe_doctor\",status:pass?\"pass\":\"fail\",message:\"@farmslot/expo-recipe doctor readiness\"}]}));})'",
    "recipe_run": "export PATH=\"${HOME}/.asdf/shims:${HOME}/.asdf/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH\"; export APP_VARIANT=\"${APP_VARIANT:-development}\" NODE_ENV=\"${NODE_ENV:-development}\" COREPACK_ENABLE_DOWNLOAD_PROMPT=\"${COREPACK_ENABLE_DOWNLOAD_PROMPT:-0}\" EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE=\"${EXPO_PUBLIC_FARMSLOT_RECIPE_BRIDGE:-1}\" WATCHER_PORT=\"${WATCHER_PORT:-{{port}}}\" CDP_PORT=\"${CDP_PORT:-9222}\"; for k in AUDIOLAB_NPM_TOKEN EAS_PROJECT_ID APPLE_TEAM_ID; do if [ -z \"$(printenv $k)\" ]; then v=$(launchctl getenv \"$k\" 2>/dev/null || true); if [ -z \"$v\" ]; then v=$(tmux show-environment -g \"$k\" 2>/dev/null | sed \"s/^$k=//\" || true); fi; if [ -n \"$v\" ]; then export \"$k=$v\"; fi; fi; done; cd {{repo}}/apps/playground && node scripts/agentic/recipe-v1/run-recipe-v1.mjs run {{recipe_path}} --artifacts-dir {{artifacts_dir}} --device \"${FARMSLOT_RECIPE_DEVICE:-{{simulator}}}\"",
    "recycle": "xcrun simctl shutdown {{simulator}}"
  },
  "health": {
    "dev_server_log": "apps/playground/.agent/metro.log",
    "dev_server_name": "Metro"
  },
  "platforms": {
    "ios": {
      "device_check": "xcrun simctl list devices booted 2>/dev/null | grep -q '{{simulator}}'",
      "recycle": null
    },
    "android": {
      "device_check": "adb devices 2>/dev/null | grep -q '{{adb_serial}}'",
      "recycle": "adb -s {{adb_serial}} shell pm clear com.audiolab"
    }
  },
  "resources": {
    "ios-sim": {
      "type": "device",
      "platform": "ios",
      "label": "iOS Sim",
      "streamable": true,
      "controllable": true,
      "watch": { "type": "process-poll", "cmd": "xcrun simctl list devices booted 2>/dev/null | grep -q '{{simulator}}'" },
      "hooks": {
        "health": "xcrun simctl list devices booted 2>/dev/null | grep -q '{{simulator}}'",
        "boot": "xcrun simctl boot '{{simulator}}'",
        "shutdown": "xcrun simctl shutdown '{{simulator}}'"
      }
    },
    "android-emu": {
      "type": "device",
      "platform": "android",
      "label": "Android Emu",
      "streamable": true,
      "controllable": true,
      "watch": { "type": "process-poll", "cmd": "adb -s {{adb_serial}} get-state 2>/dev/null | grep -q device" },
      "hooks": {
        "health": "adb -s {{adb_serial}} get-state 2>/dev/null | grep -q device",
        "boot": "$ANDROID_HOME/emulator/emulator -avd {{avd}} &",
        "shutdown": "adb -s {{adb_serial}} emu kill"
      }
    },
    "dev-server": {
      "type": "dev-server",
      "label": "Metro",
      "streamable": false,
      "controllable": true,
      "watch": { "type": "port-listen", "port": "{{port}}" },
      "hooks": {
        "health": "lsof -i :{{port}} >/dev/null 2>&1",
        "shutdown": "lsof -ti :{{port}} 2>/dev/null | xargs kill 2>/dev/null; true"
      }
    }
  },
  "recipe_run_supports_playback_slow": false
}
