# Agent Mode

- Complete the task in one pass when feasible.
- Keep `TASK.md` checkboxes current.
- If blocked, write the blocker clearly in `TASK.md` and stop.
- Do not ask for user input from inside a worker task.
